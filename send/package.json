{
  "name": "send2kobo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-09-23",
    "deploy": "wrangler pages deploy public",
    "cron:dev": "cd cron-cleanup && wrangler dev --test-scheduled",
    "cron:deploy": "cd cron-cleanup && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.78.0"
  }
}
